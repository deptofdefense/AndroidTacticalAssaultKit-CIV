
package com.atakmap.android.widgets;

public class SeekBarControlCompat {
    public static void show(SeekBarControl.Subject subject, long timeout) {
        SeekBarControl.show(subject, timeout);
    }

    public static void show(SeekBarControl.Subject subject, long timeout,
            int minIcon, int maxIcon) {
        SeekBarControl.show(subject, timeout);
    }
}
